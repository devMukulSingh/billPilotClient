{
  "name": "billManagement",
  "version": "1.0.0",
  "packageManager": "bun@1.1.42",
  "scripts": {
    "build": "tsc && vite build",
    "lint": "turbo lint",
    "dev": "dotenv -- turbo run dev"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@clerk/remix": "^4.4.23",
    "dotenv-cli": "^8.0.0"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}
